# 04_postprocess — 이미지 후가공 기록

> 근거: 과제 §4 「이미지 후가공」, §6 「품질 기준 — AI가 생성한 이미지 속 뭉개진 글자나 부자연스러운 요소를 수정 없이 그대로 제출해서는 안 된다」

프롬프트 개선(v1 → v2)만으로는 AI 이미지의 한글 렌더링 결함을 완전히 제거할 수 없었다.
따라서 v2 원본을 **최종본(final)** 으로 바로 제출하지 않고, Figma 상에서 결함을 덮는 후가공 단계를 한 번 더 거쳤다.

이 문서는 그 3단계 중 마지막 단계에 해당한다.

```
초안(v1)  →  수정(v2, 프롬프트 개선)  →  최종(final, Figma 후가공)
```

---

## 1. 후가공 방식 선택 근거

| 방식 | 채택 여부 | 사유 |
|---|---|---|
| **텍스트 레이어 덮어쓰기 (Figma)** | **채택** | 한글 텍스트 결함이 결함의 대부분. 덮어쓰면 문구가 100% 정확해지고, 3장 전체에 동일 폰트를 적용할 수 있어 톤 통일에도 유리 |
| 인페인팅 (부분 재생성) | 부분 채택 | 텍스트가 아닌 그래픽 결함(아이콘 뭉개짐, 아바타 얼굴 왜곡)에만 적용 |
| 업스케일링 | 미채택 | 원본 해상도가 1024×1536 이상으로 충분. 해상도 부족이 결함 원인이 아니었음 |
| 프롬프트 재생성 | 미채택 | 재생성 시 시드 고정이 불가능(§reference/model_notes.md)해 이미 확보한 3장의 톤 일관성이 깨짐 |

> 핵심 판단: **재생성이 아니라 덮어쓰기**를 택한 이유가 곧 "시드 미지원 모델에서 일관성을 지키는 방법"이라는 과제 목표(§3)와 직접 연결된다.

---

## 2. 화면별 결함 점검표

> 아래 「발견된 결함」 칸은 **실제 v2 PNG를 열어 확인한 내용으로 채운다.**
> 아래 항목은 GPT Image 계열에서 한글 UI를 생성할 때 반복적으로 나타나는 점검 포인트다.

### 공통 점검 항목

- [ ] 한글 글자 획이 뭉개지거나 없는 글자로 나온 곳
- [ ] 요청하지 않았는데 모델이 임의로 만들어낸 텍스트 (README 「공통 관찰」 참고)
- [ ] 하단 탭바 라벨 4개(홈 / 크루 목록 / 커뮤니티 / 마이)의 표기 정확도
- [ ] 아이콘 형태 왜곡 (돋보기, 캘린더, 뒤로가기 화살표)
- [ ] 아바타 얼굴 왜곡, 손가락·다리 개수 이상
- [ ] 3장 간 민트색(#2EC4B6) 색상 편차
- [ ] 3장 간 카드 모서리 반경·그림자 세기 편차

### 01_main

| # | 발견된 결함 | 위치 | 수정 방식 | 수정 후 |
|---|---|---|---|---|
| 1 | (예: 태그 칩 "#초보환영"의 '환' 자가 뭉개짐) | 카드 하단 태그 영역 | Figma 텍스트 레이어 덮어쓰기 | 정확한 문구로 교체 |
| 2 | | | | |

### 02_list

| # | 발견된 결함 | 위치 | 수정 방식 | 수정 후 |
|---|---|---|---|---|
| 1 | (예: 요일 토글 7개 중 일부가 다른 글자로 렌더링) | 필터 섹션 | Figma 텍스트 레이어 덮어쓰기 | 월·화·수·목·금·토·일 정확 표기 |
| 2 | | | | |

### 03_detail

| # | 발견된 결함 | 위치 | 수정 방식 | 수정 후 |
|---|---|---|---|---|
| 1 | (예: 일정 카드의 "갑천 정규 러닝 (5km)" 문구 일부 깨짐) | 러닝 일정 섹션 | Figma 텍스트 레이어 덮어쓰기 | 정확한 문구로 교체 |
| 2 | | | | |

---

## 3. Figma 덮어쓰기 실무 절차

1. v2 PNG를 프레임에 배치하고 **잠금(Lock, `⌘/Ctrl + Shift + L`)** → 이후 작업 중 이미지가 밀리지 않는다.
2. 깨진 글자 영역 위에 사각형(`R`)을 그린다.
   - 배경이 흰색이면 채우기 `#FFFFFF`
   - 카드 위라면 스포이드(`I`)로 카드 배경색을 그대로 추출해 채운다.
3. 사각형 위에 텍스트(`T`)를 얹고 정확한 한글 문구를 입력한다.
4. 아래 타이포 규격을 **3장 모두 동일하게** 적용한다.

| 요소 | 폰트 | 크기 | 굵기 | 색상 |
|---|---|---|---|---|
| 화면 제목 / 크루명 | Pretendard | 24 | Bold | `#1A1A1A` |
| 섹션 제목 (크루 소개, 러닝 일정, 멤버) | Pretendard | 18 | SemiBold | `#1A1A1A` |
| 본문 / 카드 상세 텍스트 | Pretendard | 14 | Regular | `#4A4A4A` |
| 보조 텍스트 (최근 활동, 평점) | Pretendard | 12 | Regular | `#8A8A8A` |
| 탭바 라벨 | Pretendard | 11 | Medium | 비활성 `#8A8A8A` / 활성 `#2EC4B6` |
| CTA 버튼 라벨 (참여 신청, 더 보기) | Pretendard | 16 | Bold | `#FFFFFF` |

> Pretendard가 Figma 폰트 목록에 없으면 **Noto Sans KR** 로 대체하고, 이 문서의 표를 대체 폰트명으로 수정해 기록한다.

5. 덮어쓴 텍스트 레이어는 `fix/` 접두어를 붙여 그룹화한다. (예: `fix/01_main_tags`)
   → 채점자가 "무엇을 후가공했는지" 레이어 목록만으로 확인할 수 있다.
6. 후가공 완료본을 `images/01_main_final.png` 형식으로 Export (PNG, 2x)하고, 원본 v2는 삭제하지 않고 보존한다.

---

## 4. 최종 파일 구성

| 단계 | 파일 | 제출 여부 |
|---|---|---|
| 초안 | `images/0X_..._v1.png` | 로그 근거용 (문서 내 첨부) |
| 수정 | `images/0X_..._v2.png` | 로그 근거용 (문서 내 첨부) |
| 최종 | `images/0X_..._final.png` | **산출물 ① 로 제출** |

> 과제 §2가 요구하는 "이미지 내 텍스트 깨짐이 수정된 상태"를 만족하는 것은 `_final.png` 3장이다.
> v1·v2는 삭제하지 않는다 — 개선 과정의 증거이자 로그 문서의 근거 자료다.
