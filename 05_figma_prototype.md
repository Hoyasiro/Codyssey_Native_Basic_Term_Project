# 05_figma_prototype — Figma 배치 및 Hotspot 연결 명세

> 근거: 과제 §2 「Figma 프로젝트 (1개)」, §4 「프로토타입 제작」

산출물 ③은 "이미지 3장을 올린 파일"이 아니라 **클릭하면 화면이 넘어가는 프로토타입**이어야 한다.
이 문서는 그 조립 규격과 연결 구조를 사전에 확정해, 작업자가 바뀌어도 동일한 결과가 나오도록 하는 명세다.

---

## 1. 파일 · 프레임 규격

| 항목 | 값 |
|---|---|
| Figma 파일명 | `Project A — 러닝 크루 매칭 프로토타입` |
| 프레임 프리셋 | iPhone 14 & 15 Pro (393 × 852) |
| 프레임 3개 이름 | `01_main`, `02_list`, `03_detail` |
| 배치 순서 | 좌 → 우 (main → list → detail) |
| 이미지 삽입 | 각 프레임에 `_final.png` 를 Fill 로 넣고 `Crop` 이 아닌 `Fill` 설정 |
| 시작 프레임 | `01_main` (Flow starting point 지정) |

**배치 절차**

1. `#` 키 → Frame 선택 → 우측 패널에서 iPhone 14 & 15 Pro 선택. 3개 생성.
2. `_final.png` 를 프레임 위로 드래그 → 자동으로 이미지 Fill 적용.
3. 이미지 레이어 잠금(`⌘/Ctrl + Shift + L`).
4. `01_main` 선택 → Prototype 탭 → 프레임 왼쪽의 ▷ 아이콘 클릭 → **Flow 1 시작점** 지정.

---

## 2. Hotspot 명세

각 Hotspot은 **투명 사각형**으로 만든다.
사각형을 그린 뒤 채우기 불투명도를 `0%` 로 두거나, 채우기를 지우고 이름만 남긴다.
(불투명도 0%여도 클릭은 정상 감지된다.)

| # | 출발 화면 | Hotspot 레이어명 | 덮을 영역 | 도착 화면 | 인터랙션 | 애니메이션 |
|---|---|---|---|---|---|---|
| 1 | `01_main` | `hs_main_to_list_card` | "오늘의 추천 크루" 카드 전체 | `02_list` | On tap → Navigate to | Smart animate / 300ms |
| 2 | `01_main` | `hs_main_to_list_tab` | 하단 탭바 "크루 목록" 아이콘 | `02_list` | On tap → Navigate to | Instant |
| 3 | `01_main` | `hs_main_to_list_cta` | 카드 내 "더 보기" 버튼 | `02_list` | On tap → Navigate to | Smart animate / 300ms |
| 4 | `02_list` | `hs_list_to_detail_card1` | 카드 1 "갑천 러너스" 전체 | `03_detail` | On tap → Navigate to | Move in ← / 300ms |
| 5 | `02_list` | `hs_list_to_main_tab` | 하단 탭바 "홈" 아이콘 | `01_main` | On tap → Navigate to | Instant |
| 6 | `03_detail` | `hs_detail_back` | 헤더 사진 좌상단 뒤로가기 화살표 | `02_list` | On tap → Navigate to | Move out → / 300ms |
| 7 | `03_detail` | `hs_detail_cta` | 하단 "참여 신청" 버튼 | `03_detail` | On tap → Open overlay* | Dissolve |

\* 7번은 선택 사항. 「신청이 완료되었습니다」 문구만 담은 작은 오버레이 프레임을 하나 더 만들면 흐름이 닫힌다. 시간이 부족하면 생략하고 1~6번만 연결해도 요구사항은 충족된다.

**필수 최소 연결은 1·4·6번** — 과제가 요구하는 `[메인] → [목록] → [상세] → (뒤로) [메인 흐름 복귀]` 를 이 3개가 만든다.
2·3·5번은 탭바가 실제로 동작하는 것처럼 보이게 하는 보강 연결이다.

**Hotspot 크기 원칙**

- 카드 전체를 덮는 Hotspot은 카드 외곽선에 정확히 맞춘다.
- 아이콘·버튼처럼 작은 요소는 실제 그림보다 **상하좌우 8px 정도 넉넉하게** 잡는다. 시연 중 클릭이 빗나가지 않는다.
- Hotspot끼리 겹치지 않게 한다. 겹치면 위 레이어가 클릭을 가로챈다.

---

## 3. 화면 흐름도

```
        ┌──────────────────────────────────────────┐
        │                                          │
        ▼                                          │
   [01_main] ──카드 / 더 보기 / 탭바──▶ [02_list] ──카드1──▶ [03_detail]
        ▲                                 ▲                    │
        └────────── 탭바 "홈" ─────────────┘                    │
                                          └──── 뒤로가기 ───────┘
```

---

## 4. 공유 링크 설정 (제출 직전 반드시 확인)

1. 우측 상단 **Share** 클릭.
2. 「Anyone with the link」 선택 → 권한 **can view**.
3. 링크 복사 후 **시크릿 창(로그아웃 상태)에서 직접 열어 확인.**
   → 로그인 요구 화면이 뜨면 권한 설정이 안 된 것이다. 제출 후에 발견하면 늦는다.
4. 제출 링크는 편집 화면(`/design/...`)이 아니라 **Present 모드 링크(`/proto/...`)** 를 함께 적어둔다.
   - 편집 링크: 채점자가 레이어·후가공 흔적을 확인하는 용도
   - Present 링크: 클릭 동작을 바로 시연하는 용도
   - 둘 다 적어두면 어느 쪽으로 열어도 평가가 가능하다.

---

## 5. 제출 전 자가 시연 체크

- [ ] 시크릿 창에서 Present 링크가 열린다
- [ ] `01_main` 이 시작 화면으로 뜬다
- [ ] 카드 클릭 → `02_list` 로 이동한다
- [ ] 목록 카드 클릭 → `03_detail` 로 이동한다
- [ ] 뒤로가기 클릭 → `02_list` 로 돌아온다
- [ ] 탭바 "홈" 클릭 → `01_main` 으로 돌아온다
- [ ] 프레임에 올라간 이미지가 `_v2` 가 아니라 `_final` 이다
- [ ] 어느 화면에서도 깨진 한글이 보이지 않는다
